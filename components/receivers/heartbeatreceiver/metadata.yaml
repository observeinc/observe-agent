type: heartbeat
scope_name: observe-agent/receiver/heartbeatreceiver

status:
  class: receiver
  stability:
    development: [logs]
  distributions: [contrib]
  codeowners:
    active: [alexylew]

tests:
  config:
    interval: "5m"
    environment: "linux"
